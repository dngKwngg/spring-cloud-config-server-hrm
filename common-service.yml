server:
  port: 8083

spring:
  application:
    name: common-service
  datasource:
    url: jdbc:postgresql://localhost:5433/hrm_role_permission
    username: postgres
    password: dangquang1009
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
